# Run record -- L-322 Stage D, patch D3 (Earth's pole of date, orrery)

**Built on orrery `bba21459bb5dae46d94cb650fd6ba4ab05ee0286` at
https://github.com/tonylquintanilla/palomas_orrery plus patch D2**
(`patch_L322_D_2_orrery_rows_and_poles_20260923.py`, not yet pushed at
the time of this build; D3 refuses to run without it). Gallery
`2e0fa8f5` at https://github.com/tonylquintanilla/tonyquintanilla.github.io
is not touched. Built from `BUILD_MANIFEST_L322_D_earth_pole_20260922.md`
rev 3, the revision carrying Tony's ruling to measure the tilt against
the Earth-Moon barycenter's orbit (Horizons target 3).

**Rules read:** provenance-discipline 2.18, horizons-orbital-mechanics
1.1, agentic-pre-test 1.2, protocol v3.68 in the repo.

Written September 23, 2026 by Claude Opus 5.5 for Tony Quintanilla.

## What the patch changes

Three new files and four edited ones, all or nothing.

- **`earth_pole_of_date.py`, new.** For the plot's date it fetches
  Earth's north pole (Horizons observer quantity 32, target 399, seen
  from the Sun; astroquery's columns `NPole_RA` and `NPole_DEC`, checked
  in astroquery 0.4.11's own source) and the Earth-Moon barycenter's
  osculating orbit (target 3 about the Sun, ecliptic). It computes the
  tilt as the angle between the two, after turning the pole into the
  drawing's frame with `EARTH_OBLIQUITY_J2000_DEG`. It caches both, with
  their source blocks, in `data/earth_pole_cache.json`, keyed by the day.
  When Horizons cannot be reached, or no date is given, it returns the
  fallback pair from `constants_new.py` and no tilt.
- **`test_earth_pole_of_date.py`, new.** 14 offline checks; added to the
  maintenance run as "Earth pole of date".
- **`earth_pole_live_check.py`, new.** The script Tony runs to see real
  fetches. It writes nothing.
- `idealized_orbits.py`: `create_planet_transformation_matrix('Earth')`
  reads the pole of the scene's date. Every other body is unchanged.
- `planet_visualization_utilities.py`: Earth's axis hover prints the
  tilt of date, or says why none is shown. The typed `23.44 deg` goes.
- `palomas_orrery.py`: the static plot sets the scene date to its date,
  the animation to its first frame's date, just before the center-body
  shells are built. The social export reuses the last plotted figure and
  needs no change. The two consumers in the gallery repository render
  figures already built and fetch nothing (Check All Parallel Pipelines).
- `orrery_maintenance_run.py`: the new checker line.

## Hover text, before and after

Earth's rotation axis marker, the Obliquity line:

- Before: `Obliquity: 23.44 deg`
- After, with a date and a fetch (the manifest's 2026-01-01 pole):
  `Obliquity: 23.43598 deg on 2026-01-01,` / `measured against the
  Earth-Moon orbit that day (JPL Horizons).` / `It includes Earth's small
  nod, so it differs slightly` / `from the smoothed textbook value. The
  axis also circles` / `slowly over thousands of years`
- After, with no connection: `Obliquity: not shown: Horizons could not
  be reached,` / `so the axis drawn is the frame's axis, Earth's average
  pole of the year 2000`

The slashes mark the line breaks in the hover. The tilt prints at seven
figures: Horizons prints the pole's declination to five decimal places,
seven figures, and states no uncertainty, so Rule 3's counting fallback
sets the count.

## Tests, all on throwaway copies

- **The patch refuses to run before D2**, naming D2; refuses a second
  run; and will not overwrite a new file that already exists with other
  content.
- **`test_earth_pole_of_date.py`: all 14 checks pass.**
  - The manifest's worked case reproduces: 23.43597937 against 23.43598.
  - Against ERFA (the SOFA routines astropy ships as pyerfa), for
    2000-01-01, 2026-09-23 and 2100-01-01, ERFA's own true pole and
    ecliptic of date fed through this module's conversions give ERFA's
    true obliquity to within 0.0001 arcseconds. No obliquity formula is
    typed anywhere.
  - Fallback, cache, hover words and the Earth transform each pass.
- **Each check fails by name when broken.** With the sign of one term in
  the frame rotation flipped and the Earth branch in the transform
  disabled, six checks go red: the worked case, the three ERFA dates and
  the two transform checks.
- **Maintenance run**, on HEAD plus D2 plus D3: "Earth pole of date"
  passes; the provenance scanner stays at 293 Tier-1 findings. A first
  draft added two: the live check's 10-arcsecond allowance, now sourced
  to the manifest's section 5, and a docstring sentence that typed the
  old tilt, now reworded. "Constants change" is red only because D2 was
  not committed in this sandbox. "Reset completeness" fails on the
  Windows-only colour name, as it does on the unchanged repository.
- **Headless run** of `palomas_orrery.py` on a throwaway copy: the window
  builds and runs to the time limit with no error.
- **Live path, through the builders `shell_configs.py` names:**
  - With no date: Earth's axis and the belts' pole are exactly where D2
    draws them.
  - With the scene date 2026-01-01 and the manifest's pole in the cache:
    the axis and the belts' pole both move by 0.1454 degrees, and the
    dipole cone still builds all 8 traces.

## Not tested here, and why

This sandbox cannot reach Horizons. The two queries have not been run
live by this session, and a full plot (which fetches positions) could not
be run either. `earth_pole_live_check.py` and Tony's plot are the first
live tests, and are steps 1 and 3 of the patch's printed instructions.

## Open

- The gallery half of Stage D: the cache builder fetches the same two
  things, the room reads the served pole and tilt, and the rest of
  manifest section 5. Built after Tony's live check, so its fixture can
  be taken from a real Horizons response instead of from astroquery's
  documentation.
- The monthly-wobble number from the live check replaces the manifest's
  recalled 8-arcsecond estimate.
