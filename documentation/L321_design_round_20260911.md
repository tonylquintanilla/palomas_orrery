# L-321 design round -- Earth's hover-text cross-check, decided

Built on orrery `1fa413d95e3017debe4d78a6e1fd3d62038c2cb8`
at https://github.com/tonylquintanilla/palomas_orrery (read live
2026-09-11). Gallery repo not read this session.

Tony Quintanilla, PE | Claude Fable 5.1 | 2026-09-11
Type: DESIGN (zero code). Mobile session; Tony away from his machine.
Skills loaded: provenance-discipline 2.11 (matches manifest).
Companion file: L305_gap1_read_record_20260911.md, same session.

## Tony's rulings, 2026-09-11

1. **Source references carry the URL that was used.** This is the
   Access Standard's access route, recorded rather than added: DOI link
   plus how it was reached (open full text / abstract / snippet).
2. **First slice: the magnetosphere strings** (magnetopause, bow shock,
   magnetotail hovers in `earth_visualization_shells.py` and the Earth
   entries of `shell_configs.py`). Reason: L-305 rewrites these anyway,
   so one touch, not two, and the slice stays bound to the artifact on
   the critical path. Second slice: the four date-sensitive strings.
   Pairs (`hover_text` + `tooltip` carrying the same claims) are checked
   as one.
3. **The URL does not go in the hover.** A Plotly hover cannot be
   clicked. Two tiers: the hover names the source in short form ("Shue
   et al. 1998, eq. 11"); the pinned label from the drawer tap (L-318)
   carries the live link. Orrery side: URL in `# Source:` and the store
   row. Mode 5 check owed: that a Plotly scene annotation renders an
   anchor tag on the phone.
4. **Worksheet pipeline:** citation-verification type, competitive
   pattern, one shell per prompt, schema stated in the prompt. Method
   change for the skill's next bump, not for Tony: the Source cell
   records the URL actually opened and an access verdict.
5. **A corrected string when a claim goes:** the sentence goes, the
   shape stays, nothing padded in. Where the removal leaves a hole a
   visitor would notice, one plain sentence names what is not stated
   and why (Show the Envelope, applied to prose).

## Gap, after this round

- (1) Next desk session, after the L-321 close patch and push: pull the
  two files, list the magnetosphere strings by name with their claims,
  write one worksheet prompt per shell. First thing L-321 lands.
- (2) provenance-discipline next bump: worksheet Source cell carries
  URL opened + access verdict (OPEN / ABSTRACT / SNIPPET / WALLED).
- (3) Mode 5: anchor tag in a pinned label, on the phone.

## Access Standard, both papers -- CLEARED

Tony, 2026-09-11: both full-text PDFs were downloaded without a
paywall. Access route for the store rows: open full text,
https://doi.org/10.1029/98JA01103 (Shue 1998) and
https://doi.org/10.1029/2011JA017252 (Jelinek 2012). The sandbox is
bot-blocked at Wiley, which is not a paywall; the check was Tony's.

## Tony-action

None open from this round.

Written September 2026 with Anthropic's Claude Fable 5.1.
