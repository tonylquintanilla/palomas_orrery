This is now a build request, and the relay has moved several steps. Before touching anything I'll do the session-start discipline: enumerate the full document set on disk (only the build prompt landed in my context — the manifest and handoffs it references are the actual contract and must be read from disk), then re-pin both repos and run the Gate 0 delta check the prompt asks for.Important — before I go further this needs flagging. The build prompt names `PHASE2_SYNTHESIS_MANIFEST_v2.md` as "the executable contract," plus handoff v0.3, master plan v12, and the two v1 manifests, all said to be "in documentation/." But the only files on disk are the build prompt itself and the two v0.1-era docs from last turn. The contract isn't in the upload set. Let me check whether these live in the repo's `documentation/` folder before I conclude anything — re-pinning both repos first.Both HEADs match the prompt's stated base exactly — orrery `e1b2c7f`, gallery `e864fd4`. Gate 0's "did HEAD move since the prompt was written" is a clean no; the base is as stated. Now let me look for the contract documents in the repo's `documentation/` folder.API rate-limited. Let me try fetching the named documents directly from the repo at the pinned SHA via raw (which isn't rate-limited the same way), trying the likely `documentation/` path.Good — the contract documents aren't in the upload set, but they're committed to the repo's `documentation/` folder at the pinned SHA, so they're retrievable from ground truth (and since there's no upload of them, the SHA-pinned repo copy is authoritative here). Pulling the manifest v2 and handoff v0.3 now.The manifest header reveals a discrepancy I need to flag and run down. Manifest v2 pins its **orrery** base at `c10a424` and tags its orrery source claims `[verified @c10a424]` — but the build prompt's base and current HEAD are both `e1b2c7f`. So the orrery repo moved *after* the manifest was verified. The gallery repo (`e864fd42`) — where the gates and build targets actually live — is unchanged. Let me read the rest of the manifest (the gates), then the handoff, then run Gate 0 on the orrery delta.Now the Gate 0 orrery-delta check and the served-cache schema. The manifest ports real logic from the orrery (mean elements from `orbital_elements.py`, barycenter modes from `idealized_orbits.py`) with claims tagged `[verified @c10a424]` — but HEAD is now `e1b2c7f`, so I need to confirm those port-source files didn't move under the manifest. And I need the *actual* served-cache schema at gallery `e864fd42`, since the assembler reads it and I won't build against a paraphrase.Gate 0 is clean: all seven orrery port-source files are **byte-identical** between the manifest's `c10a424` and current HEAD `e1b2c7f` — the orrery HEAD moved (doc/manifest commits), but nothing the build ports from changed, so the manifest's `[verified @c10a424]` claims hold at `e1b2c7f`. Gallery is unchanged at `e864fd42`. Base is sound.

Now the served-cache schema — the thing the assembler actually reads. Pulling it at gallery `e864fd42`.Real served schema confirmed at `e864fd42`, and it matches the manifest's claims exactly: `served_window` is `null`, index has 11 objects with **no** `halley` key (F3 gate real), and the osculating block carries precisely `epoch_jd/a_au/e/i_deg/node_deg/peri_deg/M0_deg` plus an `as_of_today` cross-check point. Let me pull the Earth entry (artifact 1), a spacecraft entry (Voyager positions shape), and the top-level `model`/`feature_configs`/`scene_features` — this is what I'll build `cache_reader.py` against.Complete Earth entry captured — artifact 1's exact data source. Confirms `as_of_today` is heliocentric km (x/y/z ~1 AU scale), features are dispatch keys (`van_allen_belts`, `atmosphere_shell`), `event_link: null` present. Top-level `model` is osculating-primary, `scene_features` are backdrop layers. Now `feature_configs.json` (F1's empty-file trap) and Voyager's positions arc shape.I now have everything verified to build against real ground truth. Before writing any code, let me load the skills the protocol requires for agentic new-code delivery and check the handoff's architecture reasoning against the manifest.Skill loaded — for these pure-CPython, Pyodide-boundary-clean assembler modules the applicable gate is py_compile + a live-dispatch smoke test on the real Earth data + the ASCII/LF check (the xvfb GUI run is for the Tkinter files, which this isn't; and artifact 1 touches no builder code, so the gallery-cache-builder layered gate isn't triggered yet). Let me glance at the handoff's architecture/L-080 sections to catch anything the manifest compressed, then build.The design intent lives in handoff §7 (L-080) and §9 (position resolution — the Kepler math I'll port). Let me read those.Now I have the full design intent. The key insight from handoff §9: the orrery draws orbit *shape* by geometric true-anomaly sweep (no time dependence) and gets the position *marker* from live Horizons — but the web assembler has no Horizons, so it must **Kepler-propagate** the marker from `M0_deg`/`epoch_jd`, and `as_of_today` is the cross-check point to validate that engine. That gives artifact 1 a concrete, ground-truth-testable correctness proof: my conic+Kepler math must reproduce Earth's stored `as_of_today` from its osculating elements. Let me grab the hover/marker AU conventions, then build.Conventions captured: km+AU hover (÷149597870.7), circles for objects / cross for info-only / square-open for barycenter, single-info-marker pattern. Before building the module structure, I'll de-risk the core position engine against ground truth — the "verify execution, not appearance" gate. If my conic+Kepler math reproduces Earth's stored `as_of_today` from its osculating elements, the engine is correct and I build around it.The engine reproduces Earth's stored `as_of_today` to **0.0 km (2.6e-11 AU)** — machine precision. That verifies the conic + Kepler propagation, the ecliptic-J2000 frame, and the AU↔km units against ground truth. The hardest correctness question in the assembler is settled before I write a line of the module structure.

This is the natural artifact-1 stage. I'll build the CPython-verifiable half of artifact 1 (my side of the acceptance gate — the Pyodide render + Mode 5 are yours), tested end-to-end against the real served cache. Let me pull the current `objects_config.json` fixture and build the package.Fixtures confirmed (config has 12 objects incl. halley; served index has 11 — F3 gate real). Building the package now. Starting with the foundation modules.Now the heart — `render_orbits.py` with the ground-truth-validated engine.Now the resolver — SceneSpec → frozen AssemblyContext, with frame rejection, date resolution, and feature dispatch.That Earth color literal is malformed (a leftover hack producing a 5-digit hex). Fixing it.Now `assemble.py` (orchestration) and the L-080 fingerprint harness.Now the end-to-end CPython test against the real served-cache fixtures — this is my side of the artifact-1 acceptance gate.