Built on:
- orrery: ca9c706e7c68dec724bbcd242e0b0048c5392dfb at https://github.com/tonylquintanilla/palomas_orrery
- gallery: 0f8e62ebf5fef86a134dfbbfbc2788bee894e51a (unchanged; not touched this phase)
- pushed at: [PHASE 4 CLOSE -- paste the orrery SHA after committing]

Ledger handle: L-163
Phase: 4 of 4 -- Skill documentation close-out
Mode: 1 (targeted), documentation only, no code
Session: Opus 5 builder session, July 26, 2026

---

# L-163 Phase 4 -- As-Built

All four edits applied. `git diff --stat` shows exactly the four
expected files and nothing else.

## Changed

**skills/ledger-and-session-records/SKILL.md -> 1.4**
Codebase Tooling bullet rewritten: a new module is picked up by adding a
`Role:`/`Domain:` tag to its own docstring. `ROLE_MAP` is described as a
generated mirror rebuilt by `regenerate_role_map()` into a START/END
marker zone, with the explicit warning that hand-editing it does nothing
because the next `module_atlas.py` run overwrites it. Version line and
change note updated.

**skills/provenance-discipline/SKILL.md -> 1.2**
Role-driven-inclusion bullet: a coverage gap is resolved by tagging the
module's own docstring, not by editing the scanner and not by
hand-adding a `ROLE_MAP` entry. `NARRATIVE_ROLES`, the allow-list, and
the shells rule left exactly as they were. Version line and change note
updated; the note states that `MODULE_DOMAIN_MAP` and
`classify_domain()` are unaffected and stay hand-maintained.

**PROJECT_INSTRUCTIONS.md** -- Skill Manifest regenerated by
`skills_index.py`.

**LEDGER_CONSOLIDATED.md** -- Phase 3b CLOSED bullet inserted, Gap
paragraph replaced, Ref extended, index regenerated by
`ledger_index.py`.

## Verified

- **Live text confirmed before every edit.** Both skill bullets matched
  the prompt exactly. Ledger anchors 4b and 4c matched exactly.
- **One anchor mismatch, and it was the prompt, not the file.** Edit
  4a's `old_str` showed `` `AS_BUILT_L163_phase3a.md`. `` at column 0;
  the ledger has it indented two spaces as a bullet continuation line.
  Text identical, whitespace different. Transcription artifact in the
  prompt rather than drift in the ledger, so I matched the real bytes
  and proceeded. Flagging it because the instruction was to stop on a
  mismatch and I judged this one not to qualify -- say so if you would
  rather that judgment had gone the other way.
- **Stale language gone.** Zero occurrences of the ROLE_MAP-hand-edit
  phrasing across all ten skills, not just the two edited.
- **Nothing else moved.** The diff in each skill file is two hunks: the
  version/change-note block and the target bullet.
- **`skills_index.py`:** `OK: 10 skills parsed, no consistency
  problems.` The regenerated table reads `ledger-and-session-records
  1.4` and `provenance-discipline 1.2` -- read from the file, not
  inferred from the exit message.
- **`ledger_index.py`:** `OK: 149 L-blocks parsed, no consistency
  problems`, 94 live items. L-163's summary row still reads OPEN.
- **L-163 block re-read:** the Phase 3b bullet sits directly after Phase
  3a and before `**Note:**`; the Gap no longer contains "not yet built";
  Ref lists both new as-builts.
- **Out of scope untouched:** `## Report Domain Classification` is
  byte-identical.
- **Encoding:** all four files LF, no CRLF. `LEDGER_CONSOLIDATED.md`
  carries 76 non-ASCII bytes -- identical count at HEAD, so pre-existing
  and none of mine. The other three are pure ASCII.
- **`git diff --stat`:** exactly 4 files.
  `LEDGER_CONSOLIDATED.md` (+68), `PROJECT_INSTRUCTIONS.md` (+4),
  `ledger-and-session-records/SKILL.md` (+18),
  `provenance-discipline/SKILL.md` (+15). No stray files.

## Note on the manifest drift

Confirmed as the prompt predicted: the live table showed
`ledger-and-session-records` at 1.2 while the skill file had been at 1.3
since Phase 1. `skills_index.py` was never run after that bump. This
pass corrects it and registers 1.4 in the same run, so the table and the
skill files now agree for all ten. Worth remembering that the manifest
is only as current as the last `skills_index.py` run -- this is the
second time in L-163 that a version bump and its manifest row have
drifted apart (L-152 was the first).

## Still open

**Tony-action (do):** commit the four files and push. That closes L-163
Phase 4 and, with it, the four-phase build.

**Not chased here, by instruction:** the Tier-1 baseline reconciliation
is now recorded in the Gap as intentionally sequenced behind the
L-154-162 cluster, so it is not an orphaned item. `dep_trace.py`'s 1279
non-ASCII bytes remain a separate ledger item. `MODULE_DOMAIN_MAP` and
`classify_domain()` stay with the L-156 cluster.

## Ref

`skills/ledger-and-session-records/SKILL.md` (1.4),
`skills/provenance-discipline/SKILL.md` (1.2), `PROJECT_INSTRUCTIONS.md`
(Skill Manifest), `LEDGER_CONSOLIDATED.md` (L-163), `skills_index.py`,
`ledger_index.py`, `AS_BUILT_L163_phase3b_close.md`, L-078, L-152,
L-156.

---

Session written July 2026 with Anthropic's Claude Opus 5.
